[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "xirang-area"
version = "0.1.0"
description = "XiRang xirang-area"
requires-python = ">=3.14"
dependencies = ['xirang-core', 'ruamel.yaml>=0.18']

[tool.setuptools.packages.find]
where = ["src"]
