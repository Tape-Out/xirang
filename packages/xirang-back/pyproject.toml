[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "xirang-back"
version = "0.1.0"
description = "XiRang tool drivers: synthesis and simulation"
requires-python = ">=3.14"

[tool.setuptools.packages.find]
where = ["src"]
