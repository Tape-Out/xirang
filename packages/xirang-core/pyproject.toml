[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "xirang-core"
version = "0.1.0"
description = "XiRang xirang-core"
requires-python = ">=3.10"
dependencies = ['pyyaml']

[tool.setuptools.packages.find]
where = ["src"]
