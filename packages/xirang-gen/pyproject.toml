[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "xirang-gen"
version = "0.1.0"
description = "XiRang xirang-gen"
requires-python = ">=3.10"
dependencies = ['xirang-core']

[tool.setuptools.packages.find]
where = ["src"]
